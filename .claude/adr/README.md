# Architecture Decision Records: Claude Code configuration

Same MADR convention as `Huginn/adr/README.md`. Scope here is different: decisions about how Claude Code itself is configured or used for this project, not Huginn's own system architecture. Those stay in `Huginn/adr/`.

Naming and status values: see `Huginn/adr/README.md`. Same rules.
